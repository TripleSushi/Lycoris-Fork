# Deepwoken Rewrite
**Module diff vs. previous snapshot: +0/-0/~10 (added/removed/changed)**
```diff
+ (changed) WeaponTest
+ (changed) IceDaggers
+ (changed) RailbladeCrit
+ (changed) WeaponFlourishTest
+ (changed) RisingShadow
+ (changed) WeaponAerialAttackTest
+ (changed) Scythe
+ (changed) MetalRain
+ (changed) BurningServants
+ (changed) IceCarve
```

**Timing diff vs. previous snapshot: +1/-0/~8 (animation: +0/-0/~8, part: +1/-0/~0)**
```diff
+ (changed) Animation : IceLance (by Blastbrean)
+ (changed) Animation : VeinTendrils (by Blastbrean)
+ (changed) Animation : KyrswynterCritRunning (by Blastbrean)
+ (changed) Animation : BloodfoulerCrit (by Blastbrean)
+ (changed) Animation : BloodfoulerCritRunning (by Blastbrean)
+ (changed) Animation : GreatMaulCrit (by Blastbrean)
+ (changed) Animation : CryptCrit (by Blastbrean)
+ (changed) Animation : MaliceCrit (by Blastbrean)
+ (added) Part : VengefulSlashes (by Blastbrean)
```

**No new features were added.**
```diff
```

**For (will be or is) former testers, this will be what has changed since the last time you've seen the script.**
**For (buyers who are going to use or is one), this is what the update logs will look like in Rewrite.**
*Usually, notes will also be attached if I have something to add.*

*Your commit ID should == "805211" when the update is fully pushed to you.*